spring:
  datasource:
    url: jdbc:mysql://localhost:3308/second_hand
    username: user
    password: admin

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
